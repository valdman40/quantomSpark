// Logs are read-only — no mutation sagas needed yet.
// Placeholder for future export, archive, or clear-log flows.
export function* logsSaga() {
  // yield takeLatest('logs/export', exportLogsSaga);
}
